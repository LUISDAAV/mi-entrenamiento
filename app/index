<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Mi Entrenamiento</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=DM+Sans:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.js"></script>
<style>
  :root {
    --orange: #FC4C02;
    --orange-dim: rgba(252,76,2,0.15);
    --orange-glow: rgba(252,76,2,0.3);
    --bg: #0f0f0f;
    --bg2: #181818;
    --bg3: #222222;
    --border: rgba(255,255,255,0.07);
    --border-orange: rgba(252,76,2,0.3);
    --text: #ffffff;
    --text2: rgba(255,255,255,0.55);
    --text3: rgba(255,255,255,0.3);
    --green: #22c55e;
    --red: #ef4444;
    --blue: #38bdf8;
    --teal: #2dd4bf;
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: 'DM Sans', sans-serif;
    font-size: 14px;
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* Noise texture overlay */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.03'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 0;
    opacity: 0.4;
  }

  .wrap { max-width: 1100px; margin: 0 auto; padding: 0 20px 60px; position: relative; z-index: 1; }

  /* NAV */
  nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 20px 0;
    border-bottom: 0.5px solid var(--border);
    margin-bottom: 32px;
    flex-wrap: wrap;
    gap: 12px;
  }

  .nav-logo {
    display: flex;
    align-items: center;
    gap: 10px;
    font-family: 'Bebas Neue', sans-serif;
    font-size: 22px;
    letter-spacing: 1px;
    color: var(--text);
  }

  .nav-logo svg { width: 28px; height: 28px; }

  .nav-links {
    display: flex;
    gap: 4px;
  }

  .nav-links a {
    padding: 6px 14px;
    border-radius: 6px;
    color: var(--text2);
    text-decoration: none;
    font-size: 13px;
    font-weight: 500;
    transition: all 0.2s;
    cursor: pointer;
  }

  .nav-links a.active, .nav-links a:hover {
    color: var(--orange);
    background: var(--orange-dim);
  }

  .nav-right {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .btn-strava {
    display: flex;
    align-items: center;
    gap: 8px;
    background: var(--orange);
    color: white;
    border: none;
    padding: 8px 16px;
    border-radius: 8px;
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
    font-family: 'DM Sans', sans-serif;
    transition: opacity 0.2s;
  }
  .btn-strava:hover { opacity: 0.85; }

  .sync-dot {
    width: 7px; height: 7px;
    border-radius: 50%;
    background: var(--green);
    box-shadow: 0 0 6px var(--green);
    animation: pulse 2s infinite;
  }
  @keyframes pulse { 0%,100% { opacity:1; } 50% { opacity:0.4; } }

  /* HERO */
  .hero {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 20px;
    margin-bottom: 28px;
    background: var(--bg2);
    border: 0.5px solid var(--border);
    border-radius: 16px;
    padding: 28px 32px;
    align-items: center;
  }

  .hero-left { display: flex; align-items: center; gap: 20px; }

  .avatar {
    width: 72px; height: 72px;
    border-radius: 50%;
    border: 2px solid var(--orange);
    object-fit: cover;
    background: var(--bg3);
    flex-shrink: 0;
    display: flex; align-items: center; justify-content: center;
    font-family: 'Bebas Neue', sans-serif;
    font-size: 26px;
    color: var(--orange);
  }

  .hero-name {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 36px;
    letter-spacing: 1px;
    line-height: 1;
    margin-bottom: 6px;
  }

  .hero-sub { color: var(--text2); font-size: 13px; }

  .hero-sync {
    display: flex;
    align-items: center;
    gap: 6px;
    margin-top: 8px;
    font-size: 12px;
    color: var(--text3);
    font-family: 'JetBrains Mono', monospace;
  }

  .hero-quote {
    background: var(--bg3);
    border: 0.5px solid var(--border-orange);
    border-radius: 12px;
    padding: 20px 24px;
    max-width: 240px;
    font-size: 13px;
    color: var(--text2);
    line-height: 1.6;
    position: relative;
  }

  .hero-quote::before {
    content: '"';
    font-family: 'Bebas Neue', sans-serif;
    font-size: 48px;
    color: var(--orange);
    position: absolute;
    top: -8px;
    left: 16px;
    line-height: 1;
    opacity: 0.7;
  }

  /* METRICS */
  .metrics-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 12px;
    margin-bottom: 24px;
  }

  .metric-card {
    background: var(--bg2);
    border: 0.5px solid var(--border);
    border-radius: 14px;
    padding: 20px 22px;
    transition: border-color 0.2s, transform 0.2s;
  }
  .metric-card:hover { border-color: var(--border-orange); transform: translateY(-2px); }

  .metric-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 14px;
  }

  .metric-icon {
    width: 32px; height: 32px;
    background: var(--orange-dim);
    border-radius: 8px;
    display: flex; align-items: center; justify-content: center;
    color: var(--orange);
    font-size: 15px;
  }

  .metric-label { font-size: 12px; color: var(--text2); font-weight: 500; }

  .metric-value {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 42px;
    letter-spacing: 1px;
    line-height: 1;
    margin-bottom: 8px;
  }

  .metric-value span { font-size: 18px; opacity: 0.6; margin-left: 2px; }

  .metric-delta {
    display: flex;
    align-items: center;
    gap: 4px;
    font-size: 12px;
    font-family: 'JetBrains Mono', monospace;
  }
  .delta-up { color: var(--green); }
  .delta-down { color: var(--red); }
  .delta-flat { color: var(--text3); }

  /* CHARTS ROW */
  .charts-row {
    display: grid;
    grid-template-columns: 3fr 2fr;
    gap: 16px;
    margin-bottom: 24px;
  }

  .chart-card {
    background: var(--bg2);
    border: 0.5px solid var(--border);
    border-radius: 14px;
    padding: 24px;
  }

  .card-title {
    font-size: 13px;
    font-weight: 600;
    color: var(--text2);
    margin-bottom: 20px;
    display: flex;
    align-items: center;
    gap: 8px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .card-title svg { color: var(--orange); }

  .chart-wrap { position: relative; width: 100%; }

  .donut-center {
    position: absolute;
    top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    pointer-events: none;
  }
  .donut-center-val {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 28px;
    line-height: 1;
    color: var(--text);
  }
  .donut-center-label { font-size: 11px; color: var(--text3); }

  .donut-legend {
    display: flex;
    flex-direction: column;
    gap: 12px;
    margin-top: 20px;
  }
  .legend-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: 13px;
  }
  .legend-dot {
    width: 10px; height: 10px;
    border-radius: 2px;
    margin-right: 8px;
    flex-shrink: 0;
  }
  .legend-left { display: flex; align-items: center; color: var(--text2); }
  .legend-right { color: var(--text); font-weight: 500; font-family: 'JetBrains Mono', monospace; font-size: 12px; }

  /* ACTIVITIES TABLE */
  .activities-card {
    background: var(--bg2);
    border: 0.5px solid var(--border);
    border-radius: 14px;
    padding: 24px;
    margin-bottom: 24px;
    overflow: hidden;
  }

  .table-wrap { overflow-x: auto; }

  table {
    width: 100%;
    border-collapse: collapse;
    min-width: 600px;
  }

  th {
    text-align: left;
    font-size: 11px;
    font-weight: 500;
    color: var(--text3);
    text-transform: uppercase;
    letter-spacing: 0.5px;
    padding: 0 12px 14px 0;
    border-bottom: 0.5px solid var(--border);
  }

  td {
    padding: 14px 12px 14px 0;
    border-bottom: 0.5px solid var(--border);
    font-size: 13px;
    color: var(--text2);
    vertical-align: middle;
  }

  tr:last-child td { border-bottom: none; }
  tr:hover td { background: rgba(255,255,255,0.02); }

  .act-name { color: var(--text); font-weight: 500; }

  .badge {
    display: inline-block;
    padding: 3px 9px;
    border-radius: 99px;
    font-size: 11px;
    font-weight: 600;
  }
  .badge-run { background: rgba(252,76,2,0.15); color: #FC4C02; }
  .badge-ride { background: rgba(56,189,248,0.15); color: #38bdf8; }
  .badge-walk { background: rgba(34,197,94,0.15); color: #22c55e; }
  .badge-other { background: rgba(255,255,255,0.08); color: var(--text2); }

  .act-icon {
    width: 30px; height: 30px;
    border-radius: 8px;
    display: inline-flex; align-items: center; justify-content: center;
    margin-right: 10px;
    font-size: 14px;
    flex-shrink: 0;
  }

  .mono { font-family: 'JetBrains Mono', monospace; font-size: 12px; }

  .fc-val {
    display: flex;
    align-items: center;
    gap: 4px;
  }

  .chevron { color: var(--text3); font-size: 12px; }

  /* INSIGHTS */
  .insights-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 12px;
    margin-bottom: 32px;
  }

  .insight-card {
    background: var(--bg2);
    border: 0.5px solid var(--border);
    border-radius: 14px;
    padding: 20px;
    transition: border-color 0.2s;
  }
  .insight-card:hover { border-color: var(--border-orange); }

  .insight-header {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 10px;
  }

  .insight-icon {
    width: 28px; height: 28px;
    border-radius: 8px;
    display: flex; align-items: center; justify-content: center;
    font-size: 14px;
  }

  .insight-title { font-size: 13px; font-weight: 600; }

  .insight-text { font-size: 12px; color: var(--text2); line-height: 1.6; }

  .insight-green .insight-icon { background: rgba(34,197,94,0.15); }
  .insight-green .insight-title { color: var(--green); }
  .insight-blue .insight-icon { background: rgba(56,189,248,0.15); }
  .insight-blue .insight-title { color: var(--blue); }
  .insight-orange .insight-icon { background: var(--orange-dim); }
  .insight-orange .insight-title { color: var(--orange); }
  .insight-teal .insight-icon { background: rgba(45,212,191,0.15); }
  .insight-teal .insight-title { color: var(--teal); }

  /* FOOTER */
  footer {
    text-align: center;
    padding-top: 32px;
    border-top: 0.5px solid var(--border);
    color: var(--text3);
    font-size: 12px;
    line-height: 2;
  }

  footer .orange { color: var(--orange); }

  /* LOADING */
  #loading {
    position: fixed; inset: 0;
    background: var(--bg);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    z-index: 100;
    gap: 16px;
    transition: opacity 0.4s;
  }

  .loader-logo {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 32px;
    color: var(--orange);
    letter-spacing: 2px;
  }

  .loader-bar {
    width: 200px; height: 2px;
    background: var(--bg3);
    border-radius: 99px;
    overflow: hidden;
  }

  .loader-fill {
    height: 100%;
    background: var(--orange);
    border-radius: 99px;
    animation: load 1.2s ease forwards;
  }
  @keyframes load { from { width: 0; } to { width: 100%; } }

  .loader-text { font-size: 12px; color: var(--text3); font-family: 'JetBrains Mono', monospace; }

  /* SECTION TITLES */
  .section-header {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 16px;
  }

  .section-header h2 {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 20px;
    letter-spacing: 1px;
    color: var(--text);
  }

  .section-header svg { color: var(--orange); }

  /* ANIMATIONS */
  .fade-in {
    opacity: 0;
    transform: translateY(12px);
    animation: fadeIn 0.5s ease forwards;
  }
  @keyframes fadeIn { to { opacity: 1; transform: none; } }

  .fade-in:nth-child(1) { animation-delay: 0.05s; }
  .fade-in:nth-child(2) { animation-delay: 0.1s; }
  .fade-in:nth-child(3) { animation-delay: 0.15s; }
  .fade-in:nth-child(4) { animation-delay: 0.2s; }

  @media (max-width: 700px) {
    .charts-row { grid-template-columns: 1fr; }
    .hero { grid-template-columns: 1fr; }
    .hero-quote { max-width: 100%; }
    .metric-value { font-size: 34px; }
  }
</style>
</head>
<body>

<div id="loading">
  <div class="loader-logo">MI ENTRENAMIENTO</div>
  <div class="loader-bar"><div class="loader-fill"></div></div>
  <div class="loader-text">Cargando datos desde Strava...</div>
</div>

<div class="wrap" id="app" style="opacity:0">

  <!-- NAV -->
  <nav>
    <div class="nav-logo">
      <svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
        <rect width="32" height="32" rx="8" fill="#FC4C02"/>
        <polygon points="16,5 11,19 14.5,19 9,27 23,13 18.5,13" fill="white"/>
      </svg>
      Mi Entrenamiento
    </div>
    <div class="nav-links">
      <a class="active">Resumen</a>
      <a>Actividades</a>
      <a>Progreso</a>
      <a>Insights</a>
    </div>
    <div class="nav-right">
      <div class="sync-dot"></div>
      <button class="btn-strava">
        <svg width="14" height="14" viewBox="0 0 20 20" fill="none"><polygon points="10,2 7,12 9.5,12 6,18 14,8 11,8" fill="white"/></svg>
        Conectado a Strava
      </button>
    </div>
  </nav>

  <!-- HERO -->
  <div class="hero fade-in">
    <div class="hero-left">
      <div class="avatar" id="avatar-img">LD</div>
      <div>
        <div class="hero-name" id="hero-name">¡Hola, Luis David!</div>
        <div class="hero-sub">Acá está tu resumen semanal ⚡</div>
        <div class="hero-sync">
          <div class="sync-dot"></div>
          <span id="last-sync">Última sincronización: cargando...</span>
        </div>
      </div>
    </div>
    <div class="hero-quote">
      La disciplina de hoy<br>es el progreso de mañana.
    </div>
  </div>

  <!-- METRICS -->
  <div class="metrics-grid" id="metrics-grid">
    <div class="metric-card fade-in">
      <div class="metric-header">
        <div class="metric-icon">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 12h18M3 6h18M3 18h18"/></svg>
        </div>
        <span class="metric-label">Kilómetros esta semana</span>
      </div>
      <div class="metric-value" id="m-dist">—<span>km</span></div>
      <div class="metric-delta" id="m-dist-delta">—</div>
    </div>
    <div class="metric-card fade-in">
      <div class="metric-header">
        <div class="metric-icon">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
        </div>
        <span class="metric-label">Tiempo en movimiento</span>
      </div>
      <div class="metric-value" id="m-time">—</div>
      <div class="metric-delta" id="m-time-delta">—</div>
    </div>
    <div class="metric-card fade-in">
      <div class="metric-header">
        <div class="metric-icon">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/></svg>
        </div>
        <span class="metric-label">Actividades</span>
      </div>
      <div class="metric-value" id="m-count">—</div>
      <div class="metric-delta" id="m-count-delta">—</div>
    </div>
    <div class="metric-card fade-in">
      <div class="metric-header">
        <div class="metric-icon">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>
        </div>
        <span class="metric-label">FC promedio (runs)</span>
      </div>
      <div class="metric-value" id="m-fc">—<span>ppm</span></div>
      <div class="metric-delta" id="m-fc-delta">—</div>
    </div>
  </div>

  <!-- CHARTS -->
  <div class="charts-row">
    <div class="chart-card fade-in">
      <div class="card-title">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M9 21V9"/></svg>
        Kilómetros por semana (últimas 8 semanas)
      </div>
      <div class="chart-wrap" style="height:220px;">
        <canvas id="weekChart" role="img" aria-label="Kilómetros por semana últimas 8 semanas"></canvas>
      </div>
    </div>
    <div class="chart-card fade-in">
      <div class="card-title">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 8v4l3 3"/></svg>
        Distribución por tipo
      </div>
      <div class="chart-wrap" style="height:160px; position:relative;">
        <canvas id="typeChart" role="img" aria-label="Distribución por tipo de actividad"></canvas>
        <div class="donut-center">
          <div class="donut-center-val" id="donut-total">—</div>
          <div class="donut-center-label">km total</div>
        </div>
      </div>
      <div class="donut-legend" id="donut-legend"></div>
    </div>
  </div>

  <!-- ACTIVITIES -->
  <div class="activities-card fade-in">
    <div class="section-header">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#FC4C02" stroke-width="2"><line x1="8" y1="6" x2="21" y2="6"/><line x1="8" y1="12" x2="21" y2="12"/><line x1="8" y1="18" x2="21" y2="18"/><line x1="3" y1="6" x2="3.01" y2="6"/><line x1="3" y1="12" x2="3.01" y2="12"/><line x1="3" y1="18" x2="3.01" y2="18"/></svg>
      <h2>Actividades recientes</h2>
    </div>
    <div class="table-wrap">
      <table>
        <thead>
          <tr>
            <th>Actividad</th>
            <th>Tipo</th>
            <th>Fecha</th>
            <th>Distancia</th>
            <th>Duración</th>
            <th>FC prom.</th>
            <th></th>
          </tr>
        </thead>
        <tbody id="activities-tbody"></tbody>
      </table>
    </div>
  </div>

  <!-- INSIGHTS -->
  <div class="section-header">
    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#FC4C02" stroke-width="2"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
    <h2>Insights automáticos</h2>
  </div>
  <div class="insights-grid" id="insights-grid"></div>

  <!-- FOOTER -->
  <footer>
    <div>Datos actualizados automáticamente desde <span class="orange">Strava</span> ⚡</div>
    <div style="color: var(--text3); font-size: 11px;">Hecho con ♥ para seguir creciendo cada día</div>
  </footer>

</div>

<script>
const ACTIVITIES_URL = './activities.json';
const ATHLETE_URL = './athlete.json';
const META_URL = './meta.json';

const COLORS = {
  run: '#FC4C02',
  ride: '#38bdf8',
  walk: '#22c55e',
  other: '#888780'
};

function getWeekStart(date) {
  const d = new Date(date);
  const day = d.getDay();
  const diff = d.getDate() - day + (day === 0 ? -6 : 1);
  d.setDate(diff);
  d.setHours(0,0,0,0);
  return d;
}

function secsToHM(s) {
  const h = Math.floor(s/3600);
  const m = Math.floor((s%3600)/60);
  return h > 0 ? `${h}h ${m}m` : `${m}m`;
}

function secsToHMS(s) {
  const h = Math.floor(s/3600);
  const m = Math.floor((s%3600)/60);
  const sec = s%60;
  return `${h}:${String(m).padStart(2,'0')}:${String(sec).padStart(2,'0')}`;
}

function fmtDate(iso) {
  return new Date(iso).toLocaleDateString('es-AR', { day:'numeric', month:'short', year:'numeric' });
}

function actType(a) {
  const t = (a.type||'').toLowerCase();
  if (t === 'run') return 'run';
  if (t === 'ride') return 'ride';
  if (t === 'walk') return 'walk';
  return 'other';
}

function actLabel(a) {
  const t = (a.type||'').toLowerCase();
  if (t === 'run') return 'Carrera';
  if (t === 'ride') return 'Ciclismo';
  if (t === 'walk') return 'Caminata';
  if (t === 'weighttraining') return 'Pesas';
  return a.type || 'Otro';
}

function deltaEl(val, unit='%') {
  if (val === null || val === undefined) return `<span class="delta-flat">— sin datos previos</span>`;
  if (Math.abs(val) < 1) return `<span class="delta-flat">= igual que la semana anterior</span>`;
  const cls = val > 0 ? 'delta-up' : 'delta-down';
  const arrow = val > 0 ? '▲' : '▼';
  return `<span class="${cls}">${arrow} ${Math.abs(Math.round(val))}${unit} vs. semana anterior</span>`;
}

async function loadData() {
  try {
    const [actRes, metaRes] = await Promise.all([
      fetch(ACTIVITIES_URL),
      fetch(META_URL).catch(() => null)
    ]);

    const activities = await actRes.json();

    if (metaRes) {
      const meta = await metaRes.json();
      const d = new Date(meta.last_sync);
      document.getElementById('last-sync').textContent =
        'Última sincronización: ' + d.toLocaleDateString('es-AR', { day:'numeric', month:'short' }) + ', ' + d.toLocaleTimeString('es-AR', { hour:'2-digit', minute:'2-digit' });
    }

    renderDashboard(activities);
  } catch(e) {
    document.getElementById('loading').innerHTML = `
      <div class="loader-logo" style="color:white">ERROR</div>
      <div style="color:rgba(255,255,255,0.4);font-size:13px;text-align:center;max-width:300px;">
        No se pudieron cargar los datos.<br>Asegurate de que activities.json existe en el repo.
      </div>`;
  }
}

function renderDashboard(activities) {
  const now = new Date();
  const thisWeekStart = getWeekStart(now);
  const lastWeekStart = new Date(thisWeekStart); lastWeekStart.setDate(lastWeekStart.getDate() - 7);

  const thisWeek = activities.filter(a => new Date(a.start_date_local) >= thisWeekStart);
  const lastWeek = activities.filter(a => {
    const d = new Date(a.start_date_local);
    return d >= lastWeekStart && d < thisWeekStart;
  });

  const runs = (arr) => arr.filter(a => a.type === 'Run');

  const sumDist = (arr) => arr.reduce((s,a) => s + (a.distance||0), 0) / 1000;
  const sumTime = (arr) => arr.reduce((s,a) => s + (a.moving_time||0), 0);
  const avgFC = (arr) => {
    const withFC = arr.filter(a => a.average_heartrate);
    if (!withFC.length) return null;
    return Math.round(withFC.reduce((s,a) => s + a.average_heartrate, 0) / withFC.length);
  };

  const tDist = sumDist(runs(thisWeek));
  const lDist = sumDist(runs(lastWeek));
  const tTime = sumTime(thisWeek);
  const lTime = sumTime(lastWeek);
  const tCount = thisWeek.length;
  const lCount = lastWeek.length;
  const tFC = avgFC(runs(thisWeek));
  const lFC = avgFC(runs(lastWeek));

  document.getElementById('m-dist').innerHTML = `${tDist.toFixed(1)}<span>km</span>`;
  document.getElementById('m-dist-delta').innerHTML = deltaEl(lDist > 0 ? ((tDist-lDist)/lDist*100) : null);
  document.getElementById('m-time').innerHTML = secsToHM(tTime);
  document.getElementById('m-time-delta').innerHTML = deltaEl(lTime > 0 ? ((tTime-lTime)/lTime*100) : null);
  document.getElementById('m-count').textContent = tCount;
  document.getElementById('m-count-delta').innerHTML = lCount > 0 ? deltaEl(((tCount-lCount)/lCount*100)) : '<span class="delta-flat">— sin datos previos</span>';
  document.getElementById('m-fc').innerHTML = tFC ? `${tFC}<span>ppm</span>` : `—<span>ppm</span>`;
  document.getElementById('m-fc-delta').innerHTML = (tFC && lFC) ? deltaEl(tFC - lFC, ' ppm') : '<span class="delta-flat">— sin datos fc</span>';

  // Weekly chart — last 8 weeks
  const weeks = [];
  for (let i = 7; i >= 0; i--) {
    const wStart = new Date(thisWeekStart);
    wStart.setDate(wStart.getDate() - i*7);
    const wEnd = new Date(wStart); wEnd.setDate(wEnd.getDate() + 7);
    const label = wStart.toLocaleDateString('es-AR', { day:'numeric', month:'short' });
    const wRuns = activities.filter(a => {
      const d = new Date(a.start_date_local);
      return d >= wStart && d < wEnd && a.type === 'Run';
    });
    const wRides = activities.filter(a => {
      const d = new Date(a.start_date_local);
      return d >= wStart && d < wEnd && a.type === 'Ride';
    });
    weeks.push({ label, runKm: sumDist(wRuns), rideKm: sumDist(wRides) });
  }

  new Chart(document.getElementById('weekChart'), {
    type: 'bar',
    data: {
      labels: weeks.map(w => w.label),
      datasets: [
        {
          label: 'Carreras',
          data: weeks.map(w => parseFloat(w.runKm.toFixed(1))),
          backgroundColor: weeks.map((_, i) => i === 7 ? '#FC4C02' : 'rgba(252,76,2,0.35)'),
          borderRadius: 5,
          borderSkipped: false
        },
        {
          label: 'Ciclismo',
          data: weeks.map(w => parseFloat(w.rideKm.toFixed(1))),
          backgroundColor: 'rgba(56,189,248,0.4)',
          borderRadius: 5,
          borderSkipped: false
        }
      ]
    },
    options: {
      responsive: true, maintainAspectRatio: false,
      plugins: { legend: { display: false }, tooltip: {
        backgroundColor: '#222',
        titleColor: '#fff',
        bodyColor: 'rgba(255,255,255,0.6)',
        borderColor: 'rgba(252,76,2,0.3)',
        borderWidth: 1,
        callbacks: { label: ctx => ` ${ctx.dataset.label}: ${ctx.parsed.y} km` }
      }},
      scales: {
        x: {
          stacked: true,
          ticks: { color: 'rgba(255,255,255,0.3)', font: { size: 11 }, autoSkip: false, maxRotation: 35 },
          grid: { display: false },
          border: { color: 'rgba(255,255,255,0.07)' }
        },
        y: {
          stacked: true,
          ticks: { color: 'rgba(255,255,255,0.3)', font: { size: 11 }, callback: v => v + ' km' },
          grid: { color: 'rgba(255,255,255,0.05)' },
          border: { display: false }
        }
      }
    }
  });

  // Donut chart
  const typeMap = {};
  activities.forEach(a => {
    const t = actType(a);
    typeMap[t] = (typeMap[t] || 0) + (a.distance || 0) / 1000;
  });

  const donutLabels = Object.keys(typeMap);
  const donutData = donutLabels.map(k => parseFloat(typeMap[k].toFixed(1)));
  const donutColors = donutLabels.map(k => COLORS[k] || COLORS.other);
  const totalKm = donutData.reduce((a,b) => a+b, 0);

  document.getElementById('donut-total').textContent = totalKm.toFixed(0);

  new Chart(document.getElementById('typeChart'), {
    type: 'doughnut',
    data: {
      labels: donutLabels,
      datasets: [{ data: donutData, backgroundColor: donutColors, borderWidth: 0, hoverOffset: 4 }]
    },
    options: {
      responsive: true, maintainAspectRatio: false,
      cutout: '68%',
      plugins: { legend: { display: false }, tooltip: {
        backgroundColor: '#222',
        titleColor: '#fff',
        bodyColor: 'rgba(255,255,255,0.6)',
        borderColor: 'rgba(252,76,2,0.3)',
        borderWidth: 1
      }}
    }
  });

  const legend = document.getElementById('donut-legend');
  const typeNames = { run: 'Carreras', ride: 'Ciclismo', walk: 'Caminatas', other: 'Otros' };
  donutLabels.forEach((k, i) => {
    legend.innerHTML += `
      <div class="legend-item">
        <div class="legend-left"><span class="legend-dot" style="background:${donutColors[i]}"></span>${typeNames[k]||k}</div>
        <div class="legend-right">${donutData[i].toFixed(1)} km</div>
      </div>`;
  });

  // Activities table
  const tbody = document.getElementById('activities-tbody');
  const icons = { run: '🏃', ride: '🚴', walk: '🚶', other: '⚡' };
  const badgeCls = { run: 'badge-run', ride: 'badge-ride', walk: 'badge-walk', other: 'badge-other' };

  activities.slice(0, 12).forEach(a => {
    const t = actType(a);
    const dist = ((a.distance||0)/1000).toFixed(1);
    const dur = secsToHMS(a.moving_time||0);
    const fc = a.average_heartrate ? Math.round(a.average_heartrate) + ' ppm' : '—';
    const iconBg = { run: 'rgba(252,76,2,0.12)', ride: 'rgba(56,189,248,0.12)', walk: 'rgba(34,197,94,0.12)', other: 'rgba(255,255,255,0.06)' };
    tbody.innerHTML += `
      <tr>
        <td><div style="display:flex;align-items:center;">
          <div class="act-icon" style="background:${iconBg[t]}">${icons[t]}</div>
          <span class="act-name">${a.name}</span>
        </div></td>
        <td><span class="badge ${badgeCls[t]}">${actLabel(a)}</span></td>
        <td class="mono">${fmtDate(a.start_date_local)}</td>
        <td class="mono">${dist > 0 ? dist + ' km' : '—'}</td>
        <td class="mono">${dur}</td>
        <td><div class="fc-val mono">${fc}</div></td>
        <td class="chevron">›</td>
      </tr>`;
  });

  // Insights
  const insights = generateInsights(activities, thisWeek, lastWeek);
  const ig = document.getElementById('insights-grid');
  insights.forEach(ins => {
    ig.innerHTML += `
      <div class="insight-card insight-${ins.color}">
        <div class="insight-header">
          <div class="insight-icon">${ins.icon}</div>
          <div class="insight-title">${ins.title}</div>
        </div>
        <div class="insight-text">${ins.text}</div>
      </div>`;
  });

  // Show app
  setTimeout(() => {
    document.getElementById('loading').style.opacity = '0';
    setTimeout(() => document.getElementById('loading').style.display = 'none', 400);
    const app = document.getElementById('app');
    app.style.opacity = '1';
    app.style.transition = 'opacity 0.4s';
  }, 1200);
}

function generateInsights(all, thisWeek, lastWeek) {
  const insights = [];
  const sumDist = (arr) => arr.filter(a=>a.type==='Run').reduce((s,a) => s+(a.distance||0),0)/1000;
  const tDist = sumDist(thisWeek);
  const lDist = sumDist(lastWeek);

  if (lDist > 0) {
    const pct = ((tDist - lDist) / lDist * 100);
    if (pct >= 10) {
      insights.push({ icon: '📈', title: 'Buen trabajo', color: 'green',
        text: `Aumentaste ${Math.round(pct)}% los kilómetros respecto a la semana anterior. Seguí así.` });
    } else if (pct <= -10) {
      insights.push({ icon: '📉', title: 'Semana más liviana', color: 'blue',
        text: `Corriste ${Math.abs(Math.round(pct))}% menos que la semana anterior. ¿Descanso activo o contratiempo?` });
    }

    if (pct > 20) {
      insights.push({ icon: '🔥', title: 'Alerta de carga', color: 'orange',
        text: `Tu volumen subió más del 20%. Asegurate de recuperar bien y no acumular fatiga.` });
    }
  }

  const thisCount = thisWeek.length;
  const lastCount = lastWeek.length;
  if (lastCount > 0 && Math.abs(thisCount - lastCount) <= 1) {
    insights.push({ icon: '🎯', title: 'Consistencia', color: 'blue',
      text: `Mantuviste ${thisCount} actividades esta semana, igual que la anterior. La consistencia es la base de todo progreso.` });
  }

  const withFC = all.filter(a => a.type==='Run' && a.average_heartrate).slice(0, 10);
  if (withFC.length >= 3) {
    const avgFC = Math.round(withFC.reduce((s,a) => s+a.average_heartrate,0)/withFC.length);
    if (avgFC < 145) {
      insights.push({ icon: '❤️', title: 'Ritmo aeróbico', color: 'teal',
        text: `Tu FC promedio en runs es ${avgFC} ppm. Estás trabajando bien en zona 2, construyendo base aeróbica.` });
    } else {
      insights.push({ icon: '💓', title: 'Carga intensa', color: 'orange',
        text: `Tu FC promedio en runs es ${avgFC} ppm. Considerá incluir más sesiones de zona 1-2 para recuperación activa.` });
    }
  }

  const longRun = all.filter(a=>a.type==='Run').sort((a,b)=>b.distance-a.distance)[0];
  if (longRun) {
    insights.push({ icon: '🏃', title: 'Tirada más larga', color: 'green',
      text: `Tu run más largo fue "${longRun.name}" con ${((longRun.distance||0)/1000).toFixed(1)} km. Muy buen trabajo de fondo.` });
  }

  if (insights.length < 3) {
    insights.push({ icon: '⚡', title: 'Seguí entrenando', color: 'orange',
      text: 'Acumulás más datos cada semana. Los insights se irán refinando con el tiempo para darte recomendaciones más precisas.' });
  }

  return insights.slice(0, 4);
}

loadData();
</script>
</body>
</html>
